//colocando um operador ternario dentro de outro

let hora = 23;
let saudacao = hora <= 11 ? "Bom dia!" : hora <= 17 ? "Boa tarde!" : "Boa noite!"  ;

console.log(saudacao);

 hora = 07;
     saudacao = hora <= 11 ? "Bom dia!" 
    : hora <= 17 ? "Boa tarde!"
    : "Boa noite!"  ;

console.log(saudacao);